# API
PORT=7432
DATABASE_URL="postgresql://quiniela:quiniela@localhost:5432/quiniela?schema=public"
JWT_SECRET="cambia_esto_por_algo_largo_y_random"

# Optional: retries for transient DB wake-up/disconnects (useful on Neon free tier)
DB_RETRY_ATTEMPTS=3
DB_RETRY_BASE_DELAY_MS=500
